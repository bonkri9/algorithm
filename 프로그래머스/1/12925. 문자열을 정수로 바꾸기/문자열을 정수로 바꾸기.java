class Solution { //Solution 이라는 클래스
    public int solution(String s) { //문자열 s 안에 이미 뭔가 들어있다. 이게 들어가면
        int answer = Integer.parseInt(s); //문자열 s를 정수형의 숫자로 변환해서 정수형 answer에 담는다.
        return answer; //answer 반환
    }
}